################################################################################
# Network Policies — Monitoring Stack
# Architecture: Prometheus + Alertmanager + kube-state-metrics in-cluster.
# Grafana, Loki, Mimir are centralized (external) — no in-cluster pods.
# Alloy runs as systemd on each VM OS — not a Kubernetes pod, no policy needed.
################################################################################
---
# Prometheus — scrape all namespaces, accept queries from in-cluster clients
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: prometheus-allow-scrape
  namespace: monitoring
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: prometheus
  policyTypes:
    - Egress
    - Ingress
  egress:
    # Scrape all namespaces (any port)
    - to:
        - namespaceSelector: {}
    # Remote-write to central Mimir (HTTPS out)
    - ports:
        - port: 443
          protocol: TCP
    # DNS
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
  ingress:
    # Alertmanager
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: alertmanager
      ports:
        - port: 9090
    # Allow pods with label prometheus.io/access to query Prometheus directly
    - from:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              prometheus.io/access: "true"
      ports:
        - port: 9090
---
# Alertmanager — accept from Prometheus, send notifications outbound
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: alertmanager-network-policy
  namespace: monitoring
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: alertmanager
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: prometheus
      ports:
        - port: 9093
  egress:
    # Slack / PagerDuty / email webhooks (HTTPS + SMTP)
    - ports:
        - port: 443
          protocol: TCP
        - port: 587
          protocol: TCP
    # DNS
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
